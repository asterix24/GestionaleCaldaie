{% extends "base.html" %}

{% block center_block %}

<div class="row-fluid">
    <h3>{{data}}</h3>

<form class="form-horizontal" action="{% url home %}" method="post" enctype="multipart/form-data">
{% csrf_token %}
    <div class="well well-small">
        <div class="controls">
            {{ form }}
        </div>
    {% if form.errors %} <div class="alert alert-error">{{form.errors}}</div> {% endif %}

    <input class="btn btn-info btn-large" type="submit" value="Carica"/>
    </div>
</form>

</div>

<hr>
<div class="tabbable">
    <ul class="nav nav-tabs">
        {% for item in items %}
		{% if forloop.first %}
			<li class="active"><a href="#{{item.setting_id}}" data-toggle="tab">{{item.setting_label}}</a></li>
		{% else %}
			<li><a href="#{{item.setting_id}}" data-toggle="tab">{{item.setting_label}}</a></li>
		{% endif %}
        {% endfor %}
    </ul>
    <div class="tab-content">
    {% for item in items %}
			{% if forloop.first %}
            <div class="tab-pane active" id="{{item.setting_id}}">
			{% else %}
            <div class="tab-pane" id="{{item.setting_id}}">
			{% endif %}
                <div class="span5 offset1">
                    <table id=show_sortable_{{item.setting_id}} class="table table-condensed table-bordered">
                        <th style="background-color: #5993A5;vertical-align: middle; color: #F8F8F8; text-align:center;font-size:1.1;font-weight:500;">
                        Mostra
                        </th>
                        <tbody>
                            {% for slide in item.show %}
                                <tr class="show_sortable_content" id="{{ slide.id }}"><td>{{slide.label}}</td></tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
                <div class="span5">
                    <table id=hide_sortable_{{item.setting_id}} class="table table-condensed table-bordered">
                        <th style="background-color: #5993A5;vertical-align: middle; color: #F8F8F8; text-align:center;font-size:1.1;font-weight:500;">
                        Nascondi
                        </th>
                        <tbody>
                            {% for slide in item.hide %}
                                <tr class="hide_sortable_content" id="{{ slide.id }}"><td>{{slide.label}}</td></tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
    {% endfor %}
    </div>
</div> <!-- /tabbable -->

{% endblock %}

{% block scripts %}
<script>


{% for item in items %}
$("#show_sortable_{{item.setting_id}}").sortable({
    placeholder: "Show items",
    items: "tr.show_sortable_content",
    connectWith: '#hide_sortable_{{item.setting_id}}',
    update: function(event,ui) {
        var r = $('#show_sortable_{{item.setting_id}}').sortable('toArray');
        r = r.join('-');
        $.ajax({
            type: "POST",
            url: "/settings/view/",
            data: 'home_view=' + r + '&csrfmiddlewaretoken={{ csrf_token }}&',
        });
        }
}).disableSelection();

$("#hide_sortable_{{item.setting_id}}").sortable({
    placeholder: "Hide items",
    items:"tr.hide_sortable_content",
    connectWith: '#show_sortable_{{item.setting_id}}',
}).disableSelection();
{% endfor %}
</script>
{% endblock %}


